name: snowflake_env
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip
  - pip:
      - snowflake-connector-python
      - python-dotenv
      - snowflake-ingest
      - snowflake-snowpark-python